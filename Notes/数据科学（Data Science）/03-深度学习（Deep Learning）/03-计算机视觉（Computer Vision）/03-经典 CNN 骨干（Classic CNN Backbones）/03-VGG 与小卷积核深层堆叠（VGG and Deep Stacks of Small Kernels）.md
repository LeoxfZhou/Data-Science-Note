---
title: VGG 与小卷积核深层堆叠（VGG and Deep Stacks of Small Kernels）
tags:
  - data-science/deep-learning/computer-vision/backbones/vgg
status: published
created: 2026-08-12
published_at: 2026-08-12
---
# VGG 与小卷积核深层堆叠（VGG and Deep Stacks of Small Kernels）
## 一、拓扑结构（Structure）

VGG 网络的整体结构非常整齐，抛弃了过去各种错综复杂的算子组合，转而由标准化的 **VGG Block** 纵向线性堆叠而成，主要有 VGG16 和 VGG19 两个经典版本。整个网络从前到后的各层级结构如下：
> [!tip] 大白话理解（Plain-language Intuition）
> VGG 的设计像用同一种小积木反复搭高楼：每次只用 `3 × 3` 卷积，阶段末统一把长宽减半。规则简单，容易计算和迁移，但楼越高，计算、显存和全连接参数也越昂贵。
![[03-VGG 与小卷积核深层堆叠（VGG and Deep Stacks of Small Kernels）-20260726172132433.png|513]]
- **输入层**：接收 224×224×3 的 RGB 彩色图像。
- **VGG Block（骨干网络）**：全网清一色采用 **3×3 卷积层**（步长 Stride=1，填充 Padding=1）和 **ReLU 激活层**进行连续串联，每个 Block 末尾衔接一个 **2×2 的最大池化层**（步长 Stride=2）。
- **尺度与通道的演进**：图像长宽每经过一个 Block 的池化层就会规整地减半（224 $\rightarrow$ 112 $\rightarrow$ 56 $\rightarrow$ 28 $\rightarrow$ 14 $\rightarrow$ 7），而卷积通道数则阶梯式翻倍增加（64 $\rightarrow$ 128 $\rightarrow$ 256 $\rightarrow$ 512）。
- **全连接分类器**：网络的尾部由三个巨型的全连接层（FC1: 4096维，FC2: 4096维，FC3: 1000维或10维）组成，最后连接 **Softmax 算子**将全局特征展平并映射为各个类别的概率分布。

## 二、 设计考量 (Rationale)

构建 VGG 时的核心哲学可以概括为两点：**“用多层小核串联替代单层大核”**与**“特征图长宽减半、通道翻倍”**。

- **小核串联的几何与参数考量**：3 个步长 1 的 `3 × 3` 卷积具有与 1 个 `7 × 7` 卷积相同的理论感受野。在输入输出通道均相同的简化假设下，权重数由 $49C^2$ 降为 $27C^2$，减少约 45%；同时获得 3 次卷积和多次 ReLU。边界效应、有效感受野和实际延迟仍依赖实现。
- **空间与通道的容量置换考量**：利用 2×2 最大池化（步长2）以等比级数降低空间维度，是为了在网络前半部分快速卸载空间计算量；而在网络深层成倍增加通道数，是为了提供足够的空间容量来存储由低阶特征组合而成的、更高维且复杂的全局语义信息。

## 三、 核心改进 (Improvements)

相比于前代的 AlexNet，VGG 在深度学习架构上做出了里程碑式的优化：

- **规整的模块化结构**：VGG 主要使用重复的 $3\times3$ 卷积和 $2\times2$ 池化，结构比 AlexNet 的双 GPU 分流更规则，便于比较不同深度配置和复用中间特征。
- **系统使用小卷积核**：VGG 展示了连续 $3\times3$ 卷积在大规模图像分类中的效果，但大卷积核并未从此消失；后续架构仍会按任务、分辨率和硬件条件选择不同核尺寸。
- **系统研究深度**：VGG 用结构相近的配置比较 11–19 个含权层网络，显示在其训练设置和深度范围内，更深配置通常更准确；不能推出深度增加必然单调改善所有模型。
- **精简了池化机制**：废弃了 AlexNet 复杂的 3×3 可重叠池化，回归到极具数学规律的 2×2 长宽减半机制，使得特征图的尺度演进路径完全透明且易于计算。

## 四、 模型缺点 (Drawbacks)

尽管 VGG 规整好用，但作为早期的深层网络，它依然留下了明显的历史局限性：

- **参数量重灾区，体量异常臃肿**：VGG16 总计约 1.38 亿的参数量中，绝大部分都被最后的巨型全连接层所霸占。这导致模型文件体积异常庞大（动辄 500MB 以上），不仅计算效率低下，且极易引发过拟合。
- **显存与计算开销巨大**：由于前几层图像的分辨率非常高，在连续通过大量密集的 3×3 卷积时，会产生极其庞大的前向传播计算量，对硬件的显存带宽造成极大的压迫。
- **继续加深的优化困难**：VGG 缺少跨层快捷路径，直接堆叠更多层可能遭遇梯度传播和退化问题。19 层是论文评估的最大配置，不是所有 VGG 式网络的物理上限；残差连接为更深网络提供了更易优化的参数化方式。
- **下采样损失空间细节**：5 次池化会显著降低分辨率，需要检测或分割头通过特征金字塔、跳跃连接、空洞卷积或上采样补偿。VGG 特征曾广泛作为检测、分割和风格迁移骨干，因此不能概括为“迁移表现乏力”。

## 五、参考资料（References）
- [Very Deep Convolutional Networks for Large-Scale Image Recognition](https://arxiv.org/abs/1409.1556)

## 六、随稿补充图示（Supplementary Source Figures）
> [!example]- 原稿中未嵌入正文的图示（Unembedded Source Figures）
> 以下图片随原稿提供，但仅凭文件名无法可靠判断其对应章节，因此保留并等待人工确认，不擅自删除。
> ![[03-VGG 与小卷积核深层堆叠（VGG and Deep Stacks of Small Kernels）-20260726171401052.png]]
